{
  "name": "chativa-workspace",
  "private": true,
  "type": "module",
  "description": "Chativa monorepo — @chativa/core + @chativa/ui",
  "author": "Hamza Agar",
  "license": "MIT",
  "homepage": "https://github.com/AimTune/chativa#readme",
  "bugs": {
    "url": "https://github.com/AimTune/chativa/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AimTune/chativa.git"
  },
  "scripts": {
    "dev": "pnpm -F sandbox dev",
    "build": "pnpm -r --filter './packages/**' build",
    "test": "pnpm -r --filter './packages/**' test",
    "test:watch": "pnpm -r --filter './packages/**' exec vitest",
    "test:coverage": "pnpm --filter '@chativa/core' --filter '@chativa/connector-dummy' --filter '@chativa/genui' exec vitest run --coverage",
    "typecheck": "pnpm -r --filter './packages/**' typecheck"
  },
  "devDependencies": {
    "jsdom":      "^28.1.0",
    "typescript": "~5.8.3",
    "vitest":     "^4.0.18"
  }
}